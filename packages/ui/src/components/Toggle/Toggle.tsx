import type { FC } from "react";
import tw from "tailwind-styled-components";

const StyledToggle = tw.div``;

export type ToggleProps = {};

export const Toggle: FC<ToggleProps> = (props) => {
  return <StyledToggle>Toggle</StyledToggle>;
};
