import { create } from "@storybook/theming";

export default create({
  base: "dark",
  brandTitle: "Axelar UI",
  brandUrl: "https://axelar.network/",
  brandTarget: "_self",
  brandImage: "/logo.svg",
});
