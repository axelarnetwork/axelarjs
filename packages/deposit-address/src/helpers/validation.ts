export function validateAddress(destinationAddress: string) {
  //TODO! actually validate here
  return destinationAddress;
}
export function validateChainIds(chainIds: string[]) {
  //TODO! actually validate here
  return chainIds;
}
