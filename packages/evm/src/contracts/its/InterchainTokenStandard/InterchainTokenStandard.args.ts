/* eslint-disable @typescript-eslint/no-explicit-any */
/**
 * This file was generated by scripts/codegen.ts
 *
 * Original abi file:
 * - node_modules/@axelar-network/interchain-token-service/artifacts/contracts/interchain-token/InterchainTokenStandard.sol/InterchainTokenStandard.json
 *
 * DO NOT EDIT MANUALLY
 */

import { encodeFunctionData } from "viem";

import type { PublicContractClient } from "../../PublicContractClient";
import ABI_FILE from "./InterchainTokenStandard.abi";

export type InterchainTokenStandardInterchainTransferArgs = {
  destinationChain: string;
  recipient: `0x${string}`;
  amount: bigint;
  metadata: `0x${string}`;
};

/**
 * Factory function for InterchainTokenStandard.interchainTransfer function args
 */
export const encodeInterchainTokenStandardInterchainTransferArgs = ({
  destinationChain,
  recipient,
  amount,
  metadata,
}: InterchainTokenStandardInterchainTransferArgs) =>
  [destinationChain, recipient, amount, metadata] as const;

/**
 * Encoder function for InterchainTokenStandard.interchainTransfer function data
 */
export const encodeInterchainTokenStandardInterchainTransferData = ({
  destinationChain,
  recipient,
  amount,
  metadata,
}: InterchainTokenStandardInterchainTransferArgs): `0x${string}` =>
  encodeFunctionData({
    functionName: "interchainTransfer",
    abi: ABI_FILE.abi,
    args: [destinationChain, recipient, amount, metadata],
  });

export type InterchainTokenStandardInterchainTransferFromArgs = {
  sender: `0x${string}`;
  destinationChain: string;
  recipient: `0x${string}`;
  amount: bigint;
  metadata: `0x${string}`;
};

/**
 * Factory function for InterchainTokenStandard.interchainTransferFrom function args
 */
export const encodeInterchainTokenStandardInterchainTransferFromArgs = ({
  sender,
  destinationChain,
  recipient,
  amount,
  metadata,
}: InterchainTokenStandardInterchainTransferFromArgs) =>
  [sender, destinationChain, recipient, amount, metadata] as const;

/**
 * Encoder function for InterchainTokenStandard.interchainTransferFrom function data
 */
export const encodeInterchainTokenStandardInterchainTransferFromData = ({
  sender,
  destinationChain,
  recipient,
  amount,
  metadata,
}: InterchainTokenStandardInterchainTransferFromArgs): `0x${string}` =>
  encodeFunctionData({
    functionName: "interchainTransferFrom",
    abi: ABI_FILE.abi,
    args: [sender, destinationChain, recipient, amount, metadata],
  });

export const INTERCHAIN_TOKEN_STANDARD_ENCODERS = {
  interchainTransfer: {
    args: encodeInterchainTokenStandardInterchainTransferArgs,
    data: encodeInterchainTokenStandardInterchainTransferData,
  },
  interchainTransferFrom: {
    args: encodeInterchainTokenStandardInterchainTransferFromArgs,
    data: encodeInterchainTokenStandardInterchainTransferFromData,
  },
};

export function createInterchainTokenStandardReadClient(
  publicClient: PublicContractClient<typeof ABI_FILE.abi>,
) {
  return {
    interchainTokenId() {
      return publicClient.read("interchainTokenId");
    },
    interchainTokenService() {
      return publicClient.read("interchainTokenService");
    },
  };
}
